# R5.5 — Production Validation con datos reales (Spec)

**Fecha creación:** 2026-04-27 · **Last revised:** 2026-04-27 (K8s amendment) · **Estado:** Approved (post-R5.4 brainstorm; 5 D-FORCEs resolved)
**Posición:** Post-R5 train. NO es parte del R5 train (R5 cerró 2026-04-27 con R5.4 ship). Es validation phase pre-comercialización.
**Duración estimada:** 18-22 días calendar (12-14 días local dual-path docker-compose + K8s + 5-6 días cloud + 1-2 días closure). Phase L docker-compose + Phase LK K8s pueden correr paralelo en máquina dev (Ryzen 9 9900X + 60 GB RAM aguanta ambos stacks ≈ 26 GB peak), recuperando ~3-4 días.
**Bumps de versión:** N/A — R5.5 NO es un product release. Output = data refresh + new docs only.
**Ship target:** Marketing-grade data sheet `Verbara Platform — Verified Capacity & SLOs` + 4 docs refrescados de "v1 provisional" → "v1 measured" + 1 PRR (Production Readiness Review) doc + 1 soak test report.

---

## Context y origen

R5.4 cerró el R5 Production Readiness Release Train 2026-04-27 con SLOs/capacity/alerts en estado "v1 provisional" — números derivados de architecture review + industry benchmarks, NO de mediciones reales. La user-decision post-R5.4 closure fue **Opción A: Pulir + correr load tests reales antes de comercializar**.

R5.5 reemplaza los números provisional por mediciones reales en un staging environment (local-first híbrido luego cloud), corre chaos engineering (cierra el gate de backup/DR runbook nunca ejercitado), corre soak testing (catch leaks que un test corto no captura), agrega synthetic monitoring continuous, y produce un Production Readiness Review (Google SRE pattern) + un data sheet marketing-grade.

### D-FORCEs resolved (post-brainstorm 2026-04-27)

1. **D-FORCE-1 staging environment:** **D híbrido** — desarrollar TODO localmente primero (cero costo + iteración rápida), replicar en cloud VM cuando todas las pruebas funcionen. Eventualmente correr ambas suites (local + cloud) y comparar resultados.
2. **D-FORCE-2 soak duration:** **24h local + 48h cloud**. El run local valida que leak detection funciona; el cloud captura comportamiento sostenido representativo.
3. **D-FORCE-3 SIP trunk:** **B — SIPp + Linphone spot-check**. Bulk testing con SIPp simulando clients; 1 spot-check manual con Linphone valida audio end-to-end. NO real telco trunk en R5.5.
4. **D-FORCE-4 chaos depth:** **B — Medium, 10 experiments**. Cubre los 10 modos de fallo más comunes en CCaaS production sin transformar Phase C en mini-projects.
5. **D-FORCE-5 K8s local production-realism (added 2026-04-27 amendment):** **Dual-path local: docker-compose + K8s local Talos cluster + Kamailio/RTPEngine SBC**. User flagged que docker-compose only era bloqueante para production-realism. Hardware verification confirmó capacidad sobrada (Ryzen 9 9900X 24 threads, 60 GB RAM, KVM/libvirt activo). Decisiones (post-AskUserQuestion 2026-04-27):
   - **Phase 0LK adicional dual-path** — docker-compose se mantiene (fast iteration + valida `getting-started.md` cold-clone path para SMB customers); Phase 0LK agrega K8s local como segunda pasada (production-realism para enterprise customers). Cubre ambos perfiles de cliente.
   - **Talos Linux on KVM** — 95% production-like (immutable OS, real etcd, multi-node), 1-3 min spinup, 1-2 GB RAM/nodo. Hardware aguanta 1 control + 3 workers ≈ 8-12 GB RAM. Sweet spot realism + iteración.
   - **Kamailio + RTPEngine SBC layer obligatorio** — research confirmó 3 deal-breakers para Asterisk-en-K8s sin SBC: SIP NAT (pod IPs no routables, REGISTER/Contact rotos), RTP port range mismatch (RTP necesita UDP 5000-50000+ vs K8s NodePort 30000-32767), session affinity (call lifecycle debe pegar al mismo pod). Sin SBC la conclusión sería falsa ("Asterisk-en-K8s no funciona"). Pattern enterprise-first: Kamailio (SIP proxy + dispatcher en hostNetwork) + RTPEngine (media proxy en hostNetwork) → Asterisk pods detrás (StatefulSet + anti-affinity).
   - **CloudNativePG operator (CNCF)** para Postgres production-grade en K8s con replicación + failover declarativos.
   - **MetalLB + Cilium + Traefik** stack (LoadBalancer + CNI + ingress).

---

## Industry research highlights (informa diseño)

### Tooling industry-standard adoptado

- **NBomber** (already shipped) — HTTP API load testing en .NET, AOT-compatible.
- **SIPp** — Industry standard SIP load tester. Sin RTP el load no es realista; con RTP scales hasta limit del Asterisk box.
- **Pumba** — Docker-native chaos engineering tool. Mata, pausa, slow disk, network chaos directo a containers. Más simple que Litmus/Chaos Mesh para Docker compose deployment (deferred a R6 cuando haya K8s).
- **Prometheus blackbox-exporter** — Self-hosted synthetic monitoring. Integra con `alerts.yml` existente. Multi-region upgrade a Grafana Cloud Synthetic en R5.6 si se justifica.
- **Linphone** — Industry softphone for spot-check audio E2E.
- **Google SRE PRR pattern** — Production Readiness Review checklist + SLO + error budget framework.

### Hardware benchmarks reales (research findings)

- Asterisk PJSIP en Xeon Quad-Core: 1,500-1,600 concurrent calls con RTP audio.
- Call recording cuts capacity significantly (~30-50%).
- CPU max load operacional: < 75% (industry standard).
- Re-INVITE direct media (P2P RTP): scales a 250+ calls @ 9% CPU on 1 GHz laptop.
- High-end multicore Xeon: 4,500+ concurrent calls (VitalPBX validation).

### Modern load test gating pattern

- **Smoke gate** (per PR): 50 VUs × 3 min (subset NBomber).
- **Full load gate** (merge a main): 30-60 min full suite.
- **Soak gate** (nightly o calendar): 2-24h al 70% peak load.
- **Endurance gate** (release validation): 24-48h sustained.

R5.5 ejecuta el `endurance gate` por primera vez. CI smoke/full integration deferred a R5.6.

---

## Principios de diseño (no-atajos)

1. **Datos reales antes de afirmar.** Si no medimos → marcamos "v2 measured pending" en lugar de inventar numero.
2. **Industry-standard tooling only.** SIPp / Pumba / blackbox-exporter / Prometheus / Grafana — todo open-source, adoptable por operators.
3. **Cada finding gets remediation.** Si C.2 chaos rompe algo, fix antes de cerrar Phase C.
4. **Reproducibility.** Todo automatizado vía scripts. Run mensual fácil después.
5. **Dual-output.** Cada doc tiene versión técnica (operators) + extracto marketing (sales).
6. **Hybrid local-first.** Desarrollar local (free + fast iteration), replicar cloud (representative datos), comparar ambos.
7. **Cero costo hasta cloud phase.** Phase 0L-E-L corren en máquina dev. Cloud VM solo durante Phase 0C-E-C (~5-7 días total).

---

## Scope total · 7 phases (Phase 0L + A + B-L + C-L + D-L + E-L + 0C/B-C/D-C/E-C + F) · ~50 tasks

### Phase 0L · Local staging environment — Docker Compose (1-2 días)

**Output ceiling reference para SMB customer scenario** (los SMBs siguen `getting-started.md` docker-compose path).

**P0L.1** Provision local environment (existing dev machine specs documented + verify free disk + free RAM)
**P0L.2** Deploy full stack on local Docker (Asterisk + Postgres + Redis + Platform.Api + Web — existing `docker-compose.full.yml`)
**P0L.3** Deploy observability stack (NEW `docker-compose.observability.yml` with Prometheus + Grafana + Loki + node_exporter)
**P0L.4** Wire `alerts.yml` to alertmanager + verify Grafana dashboards muestran meters live (gates the "alerts.yml verified in real stack" missing item)
**P0L.5** Document local staging environment in `docs/operations/staging-environment.md` § "Docker Compose variant"

### Phase 0LK · K8s Local Production-Realism Setup (NEW, ~3 días)

**Output: production-like reference para Enterprise customer scenario.** Bootstraps Talos K8s cluster + Kamailio/RTPEngine SBC + CloudNativePG + observability stack en local. Phase L (docker-compose) y Phase LK (K8s) corren en paralelo en máquina dev (~26 GB RAM peak combined, fits 60 GB).

**P0LK.1** Install tooling (~1h)
- `talosctl` (mandatory) — Talos Linux CLI
- `kind` (optional fallback si Talos bootstrap > 4h)
- `cmctl` (cert-manager CLI, install si certs needed for ingress)
- Verify `kubectl` 1.35+ + `helm` 3.20+ ya instalados

**P0LK.2** Provision Talos cluster (~3h)
- 1 control plane VM + 3 worker VMs vía libvirt/QEMU/KVM
- Configs en NEW `infra/k8s/talos/` (controlplane.yaml + worker.yaml + 3-worker patches)
- `talosctl bootstrap` + `talosctl kubeconfig`
- Verify all 4 nodes Ready

**P0LK.3** Install platform layer (~2h)
- **MetalLB** (Helm chart) — LoadBalancer service IPs en local subnet (e.g. 192.168.122.200-210)
- **Cilium CNI** (Helm chart) — eBPF networking, kube-proxy replacement
- **Traefik** (Helm chart) — ingress controller con TLS via cert-manager (opcional)

**P0LK.4** Deploy CloudNativePG operator + Postgres cluster (~3h)
- Operator install (`kubectl apply` from CNCF release)
- Cluster CR: 3 instancias HA, automated failover, PITR backup config
- Verify primary + 2 replicas Ready
- Deploy Asterisk Realtime schema migrations

**P0LK.5** Deploy Redis StatefulSet (~1h)
- Bitnami Helm chart o custom StatefulSet con persistence enabled (PVC per pod)
- Verify `PING` from pod inside cluster

**P0LK.6** Deploy Asterisk Helm chart (~4h)
- Files NEW `infra/k8s/helm/asterisk/`
- StatefulSet con anti-affinity (1 pod/node)
- ConfigMaps para `pjsip.conf`, `extensions.conf` static config
- PV (Longhorn o local-path) para voicemail/recordings
- ServiceMonitor CRD para Prometheus scraping (res_prometheus module)
- Init container con `asterisk-config` sidecar pattern (CyCoreSystems) si necesario para IP injection

**P0LK.7** Deploy Kamailio + RTPEngine SBC layer (~4h, **CRITICAL**)
- **Kamailio** en hostNetwork=true (UDP 5060), edge SIP proxy + dispatcher
- **RTPEngine** en hostNetwork=true (RTP UDP range 5000-30000), media proxy
- Dispatcher config para sticky session por call-id → mismo Asterisk pod
- 4h timebox (research-backed pattern from CyCoreSystems/asterisk-k8s-demo) — si > 4h escalate to user
- Use existing reference architecture: starting point NO greenfield

**P0LK.8** Deploy Platform.Api + Web Helm charts (~2h)
- Files NEW `infra/k8s/helm/platform/`
- Deployment + Service + Ingress
- ConnectionStrings via Secret CR pointing a CloudNativePG cluster + Redis
- HPA (Horizontal Pod Autoscaler) configurado pero scale a 1 inicial

**P0LK.9** Deploy observability stack (~3h)
- **kube-prometheus-stack** Helm chart (Prometheus Operator + Grafana + Alertmanager)
- **Loki** stack Helm chart (log aggregation)
- **blackbox-exporter** Helm chart (synthetic checks)
- ServiceMonitor CRDs para Asterisk + Platform.Api + node-exporter scraping
- Grafana datasource Loki + Prometheus auto-wired

**P0LK.10** Wire alerts.yml + verify (~2h)
- PrometheusRule CRD desde existing `docs/operations/alerts.yml`
- Alertmanager routes a console webhook (PagerDuty/Slack deferred)
- Grafana dashboards muestran K8s + Asterisk + Platform.Api metrics live
- Trigger 1 test alert (down a pod) y verify firing

**P0LK.11** Document K8s setup en `staging-environment.md` § "K8s local variant" (~2h)
- Talos bootstrap procedure
- Reproducible vía `scripts/k8s-up.sh` (creates VMs + bootstrap + installs all charts)
- `scripts/k8s-down.sh` cleanup
- Troubleshooting common issues

**P0LK.12** Ship gate · cold-bootstrap reproducible (~1h)
- Tear down + re-bootstrap from scratch
- Verify ≤ 30 min target end-to-end
- If > 30 min, document optimization opportunities + still ship

### Phase A · Test infrastructure prep (3-4 días, after Phase 0L + 0LK)

Phase A produce artefactos shared entre Phase L (docker-compose) y Phase LK (K8s) sub-runs. SIPp scenarios are platform-agnostic (target Kamailio en K8s o Asterisk directo en docker-compose). Pumba (Docker chaos) cubre Phase L, Chaos Mesh (K8s chaos) cubre Phase LK.

**A.1** Multi-tenant data seed scripts: `scripts/seed-staging.sh` que crea 3 tenants (Small/Medium/Large profiles) × 100 agents × 50 queues con realistic distribution. Resuelve el gap "load-test.sh assumes pre-seeded loadtest tenant".
**A.2** Fix `scripts/load-test.sh` tenant seed step (B.4 doc-lint finding from R5.4) — call seed-staging.sh first if tenant not present.
**A.3** SIPp scenarios for Asterisk side: 5 XML scripts en `tests/sipp-scenarios/`:
  - `01-basic-call.xml` — INVITE/ACK/BYE básico con RTP
  - `02-ivr-navigation.xml` — DTMF para menú IVR
  - `03-queue-join.xml` — caller entra a queue support, espera 30s, atendido por agent
  - `04-conference.xml` — 3-party conference setup + tear-down
  - `05-transfer.xml` — blind/attended transfer flow
**A.4** Setup `blackbox-exporter` con 5-10 critical user journeys: HTTP probes a `/health/ready`, `/api/v1/auth/login`, `/api/v1/queues`, `/api/v1/agents/me/presence`, `/scalar/v1`. Wire a alertmanager (Phase L docker-compose) + ServiceMonitor CRD (Phase LK K8s).
**A.5** Pumba chaos experiment definitions: 10 experiments documented en `tests/chaos-experiments/pumba/` (target: docker-compose Phase L):
  1. `pg-pause-30s` — pausa Postgres 30 seg
  2. `pg-kill-restart` — mata Postgres y verifica restart
  3. `redis-pause-30s` — pausa Redis 30 seg
  4. `redis-kill-restart` — mata Redis y verifica restart
  5. `asterisk-crash` — kill Asterisk container
  6. `platform-api-crash` — kill Platform.Api container
  7. `network-partition-pg` — bloquea Postgres networking 60s
  8. `slow-disk-pg` — disk latency injection 200ms
  9. `bandwidth-limit-rtp` — limit RTP bandwidth a 200 Kbps
  10. `packet-loss-rtp` — 5% packet loss en RTP traffic

**A.6 (NEW K8s)** SIPp test runner Helm chart (~3h) — `infra/k8s/helm/sipp-runner/` CronJob para reproducible runs contra Kamailio external IP (MetalLB). Same 5 scenarios, K8s-native invocation.

**A.7 (NEW K8s)** Chaos Mesh experiments (~6h) — 10 K8s-native experiments en `tests/chaos-experiments/chaos-mesh/`, paridad con Pumba pero CRD-based:
  1. `pg-pod-kill` — PodChaos against CloudNativePG primary
  2. `pg-network-partition` — NetworkChaos isolates Postgres pod
  3. `redis-pod-kill` — PodChaos against Redis StatefulSet
  4. `asterisk-pod-kill` — PodChaos against Asterisk StatefulSet (verify session affinity recovery via Kamailio)
  5. `platform-api-pod-kill` — PodChaos against Platform.Api Deployment (HPA recovery)
  6. `etcd-member-pause` — PodChaos against 1 Talos control plane etcd
  7. `cilium-cni-restart` — PodChaos against Cilium DaemonSet (CNI resilience)
  8. `kube-apiserver-pause` — PodChaos against Talos control plane (read-only fallback)
  9. `bandwidth-limit-rtpengine` — NetworkChaos limits RTPEngine bandwidth
  10. `packet-loss-kamailio` — NetworkChaos 5% loss on Kamailio SIP traffic

**A.8 (NEW K8s)** ServiceMonitor CRDs (~2h) — Prometheus Operator scraping for:
  - Asterisk res_prometheus endpoints (`/metrics` per pod)
  - Platform.Api `/metrics` per pod
  - CloudNativePG operator metrics
  - Cilium agent metrics
  - Talos node metrics

**A.9 (NEW K8s)** NetworkPolicy manifests (~3h) — security baseline para multi-tenant K8s deployment:
  - Default deny-all en namespaces aplicación
  - Allow Platform.Api → Postgres + Redis only
  - Allow Asterisk → Postgres only
  - Allow Kamailio ingress only en hostNetwork
  - Egress to internet allowed only desde Platform.Api (AgentAssist providers)

**A.10 (NEW K8s)** Backup CronJobs (~3h) — production-grade backup strategy:
  - **Velero** install (Helm chart) — full cluster backups (manifests + PVs)
  - CloudNativePG built-in PITR backup → S3 mock (MinIO local) o filesystem
  - Redis snapshot CronJob (BGSAVE → PV)
  - Schedule: daily full + hourly incremental
  - Verify restore vía `velero restore create` test

### Phase B-L · Baseline measurement LOCAL Docker Compose (2-3 días, paralelizable después de Phase A)

**B-L.1** Run NBomber suite (5 scenarios) en local docker-compose — capture HTTP API p50/p95/p99 medidos
**B-L.2** Run SIPp suite (5 scenarios) en local docker-compose — capture concurrent call limits + RTP loss + Asterisk CPU/MEM
**B-L.3** Hardware baseline characterization: `lscpu`, `free -h`, `iostat -x 5`, `nethogs` durante peak NBomber + SIPp
**B-L.4** Cross-correlate NBomber + SIPp + system metrics → identify primary bottlenecks
**B-L.5** Document local Docker Compose baseline en `docs/operations/load-test-baseline.md` § "Local Docker Compose results 2026-XX-XX"

### Phase B-LK · Baseline measurement LOCAL K8s (NEW, 2-3 días, paralelo a Phase B-L)

K8s baseline measurement contra Talos cluster. Captures K8s-specific behavior: CNI overhead (Cilium eBPF), kube-proxy latency, Service NAT (where applicable), ServiceMonitor scraping cost, Kamailio dispatcher overhead, RTPEngine media proxy cost.

**B-LK.1** Run NBomber suite contra Platform.Api Service en K8s (vía MetalLB external IP) — capture HTTP API p50/p95/p99 K8s-side
**B-LK.2** Run SIPp suite contra Kamailio external IP (MetalLB) — captures full path SBC → Asterisk pods, including dispatcher overhead
**B-LK.3** Hardware baseline en cada Talos node: `talosctl dashboard` per nodo + per-pod metrics vía kube-state-metrics
**B-LK.4** Cross-correlate: K8s metrics (kube-state-metrics + node-exporter) + Asterisk metrics (res_prometheus) + CloudNativePG operator metrics + Cilium hubble flows + Kamailio statistics
**B-LK.5** Document K8s baseline en `docs/operations/load-test-baseline.md` § "Local K8s results 2026-XX-XX"

### Phase C-L · Stress + chaos LOCAL Docker Compose (2-3 días, después de B-L)

**C-L.1** Stress test: amplifica cada NBomber scenario hasta find breaking point (200% planned target). Captures degradation curve.
**C-L.2** Pumba chaos: ejecuta los 10 experiments del catálogo A.5 (pumba/). Cada uno: pre-snapshot Postgres + run experiment + observe + post-snapshot + recovery validation.
**C-L.3** Document recovery times (RTO observed) + degradation patterns en `docs/operations/chaos-test-report-local.md`.
**C-L.4** Run chaos exercise per `backup-disaster-recovery.md` monthly cadence (closes the open gate from R5.4 S5.8). First exercise log entry en `dr-exercises.md`.

### Phase C-LK · Stress + chaos LOCAL K8s (NEW, 2-3 días, después de B-LK)

**C-LK.1** Stress test K8s pods (find pod-level + cluster-level breaking point). HPA behavior observation: ¿escala correctamente bajo carga?
**C-LK.2** Chaos Mesh experiments: ejecuta los 10 experiments de A.7 (chaos-mesh/). CRD-based execution con `kubectl apply` workflow.
**C-LK.3** **K8s-specific chaos:** etcd member failure (verify HA), kube-apiserver pause (verify graceful read-only), Cilium CNI restart (verify connectivity recovery), CloudNativePG primary failover (verify automated promotion).
**C-LK.4** Document K8s recovery times + K8s-specific recovery patterns en `docs/operations/chaos-test-report-k8s-local.md`.

### Phase D-L · Soak/endurance LOCAL Docker Compose (24h continuous)

**D-L.1** Run 24h soak at 70% peak load (NBomber + SIPp combinados) en docker-compose local
**D-L.2** Monitor memory/connections/latency drift cada 1h via Prometheus
**D-L.3** Verifica que synthetic checks (E-L.1) siguen verde durante el run
**D-L.4** Document local docker-compose leak findings + drift baseline en `docs/operations/soak-test-report-local.md`

### Phase D-LK · Soak/endurance LOCAL K8s (NEW, 24h continuous, paralelo a D-L)

**D-LK.1** Run 24h soak en Talos cluster local con NBomber + SIPp combinados (vs Kamailio + Platform.Api Service)
**D-LK.2** Monitor K8s pod restarts + Postgres operator failovers + Cilium connectivity + ServiceMonitor scraping latency cada 1h
**D-LK.3** Verifica que synthetic checks (E-LK.1) siguen verde durante el run en K8s
**D-LK.4** Document K8s leak findings + K8s-specific drift en `docs/operations/soak-test-report-k8s-local.md`

### Phase E-L · Synthetic monitoring continuous LOCAL Docker Compose (1 día, paralelo D-L)

**E-L.1** Configure blackbox-exporter con 5-10 journeys (definidos en A.4), wire a alertmanager docker-compose
**E-L.2** Verify synthetic checks fire alerts cuando endpoints fallen (induce failures vía Pumba)
**E-L.3** Run synthetic checks durante D-L 24h soak — verify continuous coverage
**E-L.4** Document synthetic monitoring setup en `docs/operations/synthetic-monitoring.md` § "Docker Compose variant"

### Phase E-LK · Synthetic monitoring continuous LOCAL K8s (NEW, 1 día, paralelo D-LK)

**E-LK.1** Configure blackbox-exporter en K8s + ServiceMonitor para synthetic checks (mismas 5-10 journeys que E-L pero target K8s endpoints)
**E-LK.2** Verify Alertmanager K8s envia alertas correctamente (PrometheusRule CRD firing)
**E-LK.3** Run synthetic checks durante D-LK 24h soak K8s
**E-LK.4** Document K8s synthetic monitoring setup en `docs/operations/synthetic-monitoring.md` § "K8s variant"

### **🔄 Cloud replication checkpoint** (between Phase E-L/E-LK and Phase 0C)

Antes de provisionar cloud:
- ✅ Phase L (docker-compose) + Phase LK (K8s) **AMBOS green** — scripts run end-to-end sin manual intervention en docker-compose Y K8s local
- ✅ Outputs documentados en docs/operations/ files con secciones separadas "Local Docker Compose results" + "Local K8s results"
- ✅ Cualquier P0/P1 finding fixed inline; P2/P3 tracked como v1.13.x tickets
- ✅ Local docker-compose baseline = "Output ceiling for SMB customer scenario"
- ✅ Local K8s baseline = "Production-realistic reference for Enterprise customer scenario"
- ✅ Si Phase 0LK reveals que K8s+SBC pattern necesita ajustes (e.g., Kamailio dispatcher config issue, RTPEngine performance ceiling), esos ajustes se aplican antes del cloud spend

### Phase 0C · Cloud staging replication (~half-day si scripts buenos, hasta 1 día) — 🛑 DEFERRED INDEFINITELY 2026-05-25

> **Maintainer directive 2026-05-25:** Cloud spend (AWS/GCP/Azure) blocked pre-revenue. Phase 0C + B-C + C-C + D-C + E-C all deferred indefinitely. R5.5 closes with docker-local + K8s-local datasets only. Cloud comparison dataset, multi-AZ Redis JWT validation, and cloud-K8s sprint (R5.6) all wait for first paying customer. Tasks below preserved verbatim as ready-to-execute reference.



**P0C.1** Provision cloud VM: AWS EC2 c5.4xlarge (16 vCPU, 32 GB RAM, 200 GB SSD) o GCP e2-standard-16 equivalent. Ubuntu 24.04 LTS.
**P0C.2** Open ports: 22 (SSH) + 80/443 (Web) + 5060/udp (SIP) + 5070-5099/udp (RTP). SSH key auth only.
**P0C.3** Auto-shutdown cron: budget guard que mata VM después de 7 días o si costo proyectado > $80.
**P0C.4** Replicate stack via existing scripts (clone repos + docker compose up + seed-staging.sh)
**P0C.5** Document cloud environment specifics en `docs/operations/staging-environment.md` § "Cloud variant"

### Phase B-C / C-C / D-C / E-C · Cloud measurement runs (3-4 días)

Ejecuta el mismo conjunto de scripts de Phase B-L + C-L + D-L + E-L pero contra cloud VM:

**B-C.1-5** Cloud baseline NBomber + SIPp + hardware + correlation + document
**C-C.1-3** Cloud stress + 10 chaos experiments + document recovery (chaos-test-report-cloud.md)
**D-C.1-4** **48h cloud soak** (vs 24h local) — captures sostenido behavior representative de production
**E-C.1-3** Cloud synthetic monitoring + alert firing validation

### Phase F · Refresh + closure (2 días después de Phase E-C, **3 datasets a integrar**)

Phase F ahora consume **3 datasets**: docker-compose local (Phase L), K8s local (Phase LK), cloud docker-compose (Phase C). Esto produce documentación de capacity por **deployment pattern**, no solo por hardware tier.

**F.1** Refresh `docs/operations/slos.md` v1 provisional → **v1 measured**:
  - Cloud B-C/D-C numbers como authoritative para single-tenant deployment (representative production)
  - Local Docker Compose B-L numbers como "ceiling SMB single-host" reference
  - Local K8s B-LK numbers como "Enterprise multi-tenant K8s" reference
  - Banner "v1 provisional" REMOVED + footnote "Refreshed from R5.5 measured baseline 2026-XX-XX (3 datasets: docker-local + K8s-local + cloud)"
  - **NEW section §"K8s SBC overhead"** — quantifies Kamailio + RTPEngine latency adicional vs Asterisk-direct
**F.2** Refresh `docs/operations/capacity-planning.md` v1 provisional → **v1 measured**:
  - 4 tier limits (Small/Medium/Large/XL) con números reales del cloud baseline
  - Hardware sizing: usar c5.4xlarge specs como "Medium tier reference"
  - **NEW section §"K8s deployment tier"** — separate sizing tables for K8s 3-node cluster (control plane + 2-3 workers; per-pod resource requests/limits)
  - **NEW section §"Deployment pattern selection"** — guidance "when SMB docker-compose fits / when Enterprise K8s needed"
**F.3** Update `docs/operations/alerts.yml` thresholds basado en observed normal/peak ranges (P50 + 3σ pattern)
  - **NEW K8s-specific alerts:** PodCrashLooping (>3 restarts/15min), PVCNotBound (>5min), CloudNativePGFailover, CiliumAgentDown, EtcdQuorumLoss
**F.4** Cold-clone smoke test: clean machine + clone repos + sigue `docs/getting-started.md` cold + measure tiempo real (10/30/60 min targets validated)
**F.5** Production Readiness Review: NEW `docs/operations/production-readiness-review.md` siguiendo Google SRE pattern — checklist completo con cada item linkeado a evidence (test output, doc, alert config, etc.). **PRR cubre BOTH docker-compose AND K8s deployment patterns** — separate checklist sub-sections.
**F.6** Marketing data sheet: NEW `docs/operations/data-sheet-r5.5.md` — extracto marketing-grade con números cloud-validated, formato sales-ready. **Sells two deployment patterns:** "Validated for docker-compose (SMB) + K8s (Enterprise) deployment patterns. Same product, customer-pick infrastructure."
**F.7** Memory file: `~/.claude/projects/.../memory/project_r55_validation_data_shipped.md` con post-validation snapshot (3 datasets summary)
**F.8** Update Pro `docs/roadmap.md` con R5.5 entry + R6 brainstorm pending (cloud-K8s validation + multi-region deferred)
**F.9** Update CLAUDE.md cross-repo si test counts cambian (improbable; no source code changes)
**F.10** Cloud VM teardown (cost containment) — preserve cloud findings via S3 export
**F.11** Plan moves active/ → completed/ + commit summary

### Closure deliverables · F.10-F.11 (post-Phase F)

**F.10** Cloud VM teardown (cost containment) — preserva snapshots de cloud findings en S3 / repo
**F.11** Plan moves active/ → completed/ + commit summary

---

## Phase structure rationale

- **Phase 0L first** — sin staging real no hay nada que medir. Local cero-costo permite iterar sin presión $.
- **Phase A para infra** — sin tenant seed + SIPp + chaos catalog + synthetic checks no podemos correr B-E correctamente.
- **Phase B-L baseline antes que C-L/D-L/E-L** — necesitamos saber el "normal" antes de stress/chaos/soak.
- **Phase D-L 24h corre paralelo a Phase E-L synthetic** — pueden compartir staging sin conflicto.
- **Cloud replication checkpoint mandatory** — si Phase L tests fallan, hacer cloud es desperdicio. Cloud solo cuando local works clean.
- **Phase D-C 48h vs D-L 24h** — diferencia explica leaks que solo aparecen sostenido. Cloud is "representative production", local is "uncontested ceiling".
- **Phase F final consolidation** — refresca docs con BOTH datasets (local + cloud) clearly distinguished.

---

## Cross-repo coordination

| Repo | Cambios esperados R5.5 |
|---|---|
| Verbara.Sdk | Probable 0 (validation only) — pero si surgen issues en SDK durante chaos/soak, podrían surgir patches v1.15.2 |
| Verbara.Sdk.Pro | Probable 0 — pero si surgen leaks o issues durante D-L/D-C, fix → 1.15.1-pro patch |
| Verbara.Platform | Phase A scripts + docs/operations/ updates + likely some bug fixes from chaos/soak findings |
| Verbara.Platform.Web | Probable 0 — Web no se testea directamente en R5.5 (HTTP API + Asterisk son el target) |

**No version bumps** unless code fixes are required. R5.5 ships data, not features. Bumps reservados para follow-up patches v1.15.x si los findings lo requieren.

---

## Infrastructure stack canonical

```
Local staging Docker Compose (Phase 0L + B-L + C-L + D-L + E-L):
  - Host: dev machine (existing — verify free disk ≥ 50 GB + free RAM ≥ 16 GB)
  - OS: existing (Linux/macOS)
  - Docker 29.4+ + Compose v2 (already installed)
  - Open ports localhost: 80, 443 (Web), 5060/udp (SIP), 5070-5099/udp (RTP), 9090 (Prometheus), 3000 (Grafana)

Local staging K8s (Phase 0LK + B-LK + C-LK + D-LK + E-LK):
  - Host: dev machine — Ryzen 9 9900X 24 threads + 60 GB RAM verified (35 GB available)
  - Hypervisor: KVM/libvirt (kvm_amd module loaded, libvirtd active)
  - K8s distro: Talos Linux (immutable, production-grade)
  - Cluster: 1 control plane + 3 workers as KVM VMs (~12 GB RAM allocated, ~10% CPU baseline)
  - Tooling: talosctl + kubectl 1.35.1 + helm 3.20 + kind (fallback only)
  - CNI: Cilium (eBPF, kube-proxy replacement)
  - LoadBalancer: MetalLB (L2 mode, IP pool 192.168.122.200-210 default)
  - Ingress: Traefik (TLS via cert-manager opcional)
  - Storage: OpenEBS Local PV (replication-free) o Longhorn (HA)
  - Postgres: CloudNativePG operator (CNCF, 3-instance HA cluster con auto-failover)
  - Redis: Bitnami Helm chart o custom StatefulSet
  - Asterisk: StatefulSet anti-affinity (1 pod/node) + ConfigMap static config + PV voicemail/recordings
  - SBC layer (CRITICAL — production CCaaS pattern):
    * Kamailio (hostNetwork=true, edge SIP proxy + dispatcher with sticky sessions)
    * RTPEngine (hostNetwork=true, media proxy with RTP rewrites)
    * Reference: CyCoreSystems/asterisk-k8s-demo
  - Observability: kube-prometheus-stack Helm + Loki + blackbox-exporter + ServiceMonitor CRDs
  - Backup: Velero (cluster) + CloudNativePG PITR (Postgres) + Redis snapshot CronJob
  - Total RAM allocation: ~26 GB peak (Talos 10 GB + observability 4 GB + apps 12 GB), fits 60 GB con margen para docker-compose paralelo

Cloud staging (Phase 0C-E-C):
  - Host: AWS EC2 c5.4xlarge (16 vCPU, 32 GB RAM, 200 GB gp3 SSD)
    OR equivalent GCP e2-standard-16 / Azure Standard_D16s_v5
  - OS: Ubuntu 24.04 LTS
  - Docker 28+ + Compose v2 (auto-installed via cloud-init)
  - SSH key auth only; security group restricts ingress
  - Auto-shutdown cron (budget guard)

Application stack (existing docker-compose.full.yml + adjustments):
  - Asterisk 22 LTS (existing image)
  - Postgres 17
  - Redis 8
  - Platform.Api (built from main HEAD)
  - Web (built from main HEAD)
  - 3 demo tenants seeded via NEW scripts/seed-staging.sh

Observability stack (NEW docker-compose.observability.yml):
  - Prometheus (scrape Platform.Api + node_exporter + Asterisk metrics)
  - Grafana (dashboards: Asterisk Ecosystem Full + node + alerts)
  - Loki (log aggregation; reads Docker JSON logs)
  - node_exporter (host-level metrics)
  - blackbox-exporter (synthetic checks)
  - alertmanager (routes alerts.yml to console webhook for staging; PagerDuty/Slack integration deferred)

Test tooling (NEW + existing):
  - NBomber 6.1.0 (already shipped en tests/Verbara.Platform.LoadTests/)
  - SIPp 3.6+ (apt install sipp; scenarios in NEW tests/sipp-scenarios/)
  - Pumba 0.10+ (Docker chaos; scripts in NEW scripts/chaos/)
  - Linphone 5+ (manual spot-check audio; install instructions en docs/operations/first-deploy.md ya existe)
```

---

## Ship criteria R5.5

R5.5 se declara COMPLETE cuando:

### Data refreshed
- ✅ `docs/operations/slos.md` v1 provisional → v1 measured (banner removed, footnote "Refreshed from R5.5 cloud baseline 2026-XX-XX")
- ✅ `docs/operations/capacity-planning.md` v1 provisional → v1 measured (4 tiers con números reales)
- ✅ `docs/operations/alerts.yml` thresholds tuned basado en observed ranges
- ✅ `docs/operations/load-test-baseline.md` populated con NBomber + SIPp results both local + cloud sections

### New deliverables
- ✅ `docs/operations/staging-environment.md` con 3 sections: Docker Compose variant + K8s local variant + Cloud variant
- ✅ `docs/operations/soak-test-report-local.md` (24h docker-compose findings)
- ✅ `docs/operations/soak-test-report-k8s-local.md` (24h K8s local findings, NEW)
- ✅ `docs/operations/soak-test-report-cloud.md` (48h cloud findings)
- ✅ `docs/operations/chaos-test-report-local.md` (10 Pumba experiments findings)
- ✅ `docs/operations/chaos-test-report-k8s-local.md` (10 Chaos Mesh experiments findings, NEW)
- ✅ `docs/operations/chaos-test-report-cloud.md` (10 experiments findings)
- ✅ `docs/operations/synthetic-monitoring.md` con 2 sections: Docker Compose variant + K8s variant
- ✅ `docs/operations/dr-exercises.md` con primer chaos exercise documentado (closes R5.4 S5.8 open gate)
- ✅ `docs/operations/production-readiness-review.md` cubriendo BOTH docker-compose + K8s deployment patterns (Google SRE PRR pattern)
- ✅ `docs/operations/data-sheet-r5.5.md` (marketing-grade extract; sells 2 deployment patterns SMB + Enterprise)

### New tooling shipped — Docker Compose path (Phase L)
- ✅ `scripts/seed-staging.sh` (multi-tenant data seed)
- ✅ `scripts/load-test.sh` updated (tenant seed gap fixed)
- ✅ `scripts/sipp-test.sh` (NEW; runs SIPp suite)
- ✅ `scripts/chaos-test.sh` (NEW; runs Pumba experiments)
- ✅ `tests/sipp-scenarios/` (5 XML scenarios — shared con K8s)
- ✅ `tests/chaos-experiments/pumba/` (10 Pumba experiment definitions for docker-compose)
- ✅ `docker/docker-compose.observability.yml` (Prometheus + Grafana + Loki + exporters)

### New tooling shipped — K8s path (Phase LK, NEW)
- ✅ `infra/k8s/talos/` (controlplane.yaml + worker.yaml + 3-worker patches)
- ✅ `infra/k8s/helm/asterisk/` (Helm chart Asterisk + Kamailio + RTPEngine SBC layer)
- ✅ `infra/k8s/helm/platform/` (Helm chart Platform.Api + Web)
- ✅ `infra/k8s/helm/observability/` (kube-prometheus-stack values + Loki + blackbox-exporter)
- ✅ `infra/k8s/helm/sipp-runner/` (CronJob for reproducible SIPp runs in cluster)
- ✅ `infra/k8s/manifests/cloudnativepg/` (operator install + Cluster CR with HA + PITR)
- ✅ `tests/chaos-experiments/chaos-mesh/` (10 Chaos Mesh experiment manifests)
- ✅ `scripts/k8s-up.sh` (bootstraps Talos cluster + installs all charts; ≤30 min target)
- ✅ `scripts/k8s-down.sh` (tears down cluster cleanly)
- ✅ NetworkPolicy manifests (security baseline multi-tenant)
- ✅ Velero install + Postgres backup CronJob

### Functional gates
- ✅ Cold-clone smoke (real `docker compose up` cold) ≤ 12 min target met (or doc fix-pass applied)
- ✅ NBomber suite reproducible vía `scripts/load-test.sh` post tenant-seed fix
- ✅ SIPp suite reproducible vía `scripts/sipp-test.sh`
- ✅ Pumba chaos workflow reproducible vía `scripts/chaos-test.sh` (Phase L)
- ✅ **K8s cluster cold-bootstrap** reproducible vía `scripts/k8s-up.sh` ≤ 30 min target (NEW)
- ✅ **Chaos Mesh experiments** reproducible vía `kubectl apply` workflow (Phase LK, NEW)
- ✅ **Kamailio + RTPEngine SBC layer** functional — SIPp suite contra Kamailio external IP delivers calls a Asterisk pods con sticky session affinity (NEW)
- ✅ **CloudNativePG primary failover** observed working — primary pod kill triggers automated promotion ≤ 30s (NEW)
- ✅ Synthetic monitoring active + alerts firing on staging (Docker Compose + K8s ambos verified end-to-end)
- ✅ All 10 chaos experiments executed Phase L (Pumba) + Phase LK (Chaos Mesh) + Phase C cloud, recovery times documented per environment
- ✅ 24h local docker-compose + 24h K8s local + 48h cloud soak completed without P0 leak

### Findings remediation
- Cualquier P0 finding → fix inline (puede inflar timeline)
- P1 finding → assess if blocker; default fix inline
- P2/P3 → tickets v1.13.x patches (no bloquean R5.5 closure)

---

## Cross-cutting discipline (aplica a TODAS las phases)

1. **Reproducibility:** todo automatizado vía scripts. NO manual one-off commands.
2. **Pre-snapshot before each chaos experiment:** Postgres + Redis snapshot pre + post + verified restorable.
3. **Prometheus retention:** 30 días local, 7 días cloud (cost-aware).
4. **Grafana dashboards exported as JSON:** committed to repo, can be re-imported.
5. **Cost monitoring:** AWS billing alarm at $30, $50, $80 thresholds. Auto-shutdown if exceeds.
6. **Findings-as-issues:** every chaos break / soak leak / synthetic fail → GitHub issue con label `r5.5-validation`.
7. **No production data:** staging usa fake data only. Explicit gate F.4 verifies.

---

## Out-of-scope explícito (decisiones, no olvidos)

| Item | Por qué NO en R5.5 | Destino |
|---|---|---|
| ~~K8s deployment + Chaos Mesh~~ | **MOVED IN-SCOPE 2026-04-27** via D-FORCE-5 K8s amendment (Phase 0LK + Phase LK sub-runs) | — |
| **Cloud-K8s validation (managed K8s en AWS EKS / GCP GKE / Azure AKS)** | Phase 0LK valida K8s pattern localmente; cloud-K8s incremental (managed control plane) puede deferirse | R5.6 cloud-K8s sprint |
| **Multi-region staging** | Single-region cloud VM enough for v1 baseline | R5.6 si cliente multi-region |
| **Real SIP trunk validation (Twilio/Telnyx)** | $$ + telco coordination; SIPp + Linphone cubre el 95% | Spot-check post-R5.5 ad-hoc si requirement aparece |
| **External pen-test (vendor)** | Internal audit R5.4 baseline ya hecho | Cuando cliente enterprise lo pida formalmente |
| **Multi-day soak > 48h** | 48h cubre la mayoría de leaks; >48h tiene rendimientos decrecientes | R5.6 si aparece leak no-detected |
| **WFM forecasting / IVR Visual Designer / Encryption at-rest** | Features, no validation | R6 / 2.0-pro |
| **CI smoke/full integration (PR gate + merge gate)** | Requires CI runner pesado + budget; staging snapshot suficiente para v1 | R5.6 cuando CI infra disponible |
| **PagerDuty / Slack alertmanager integration** | alertmanager con console webhook suficiente para v1; integraciones requieren service accounts | R5.6 si operators piden |
| **Performance regression baseline en CI** | R5.6 cuando CI runner disponible | R5.6 |

---

## Riesgos identificados + mitigación

| Riesgo | Probabilidad | Impacto | Mitigación |
|---|---|---|---|
| Local staging usa demasiado RAM/disk de dev machine | Alta | Bajo | Documenta requirements (16 GB RAM + 50 GB disk free) en P0L.1; gate antes de Phase B-L |
| Cloud VM costo excede $80 (Phase D-C 48h soak corre más por bug) | Media | Bajo ($) | Auto-shutdown cron en P0C.3 + AWS billing alarm |
| Phase B-L reveals NBomber HTTP scenarios irrelevant if real bottleneck en Asterisk | Media | Medio | Por eso A.3 SIPp scenarios — cubre el lado complementario; B-L.4 cross-correlation reveals which side |
| Phase D-L/D-C soak descubre leak severo que requiere Pro/Platform code fix | Media | Alto | Si fix < 1 día → inline; si > 1 día → defer R5.6 + document leak; spec doesn't promise fix |
| Pumba chaos rompe staging recurrently y bloquea Phase D | Media | Medio | Phase C-L/C-C después de B-L/B-C baseline; pre-snapshot Postgres antes de cada chaos exp; Phase D corre AFTER Phase C closure |
| Cold-clone smoke F.4 falla con setup wizard como B.4 ya descubrió | Alta | Bajo | Fix inline en Phase A (A.2 already addresses) |
| Local + cloud datasets divergen significativamente | Alta | Medio | EXPECTED — 3 datasets se documentan separadamente (docker-local + K8s-local + cloud). Cloud es authoritative para SLOs/capacity SMB tier; K8s-local es authoritative para Enterprise tier; docker-local es "ceiling SMB single-host" comparison |
| 48h cloud soak acapara staging mientras E-C synthetic monitoring corre | Baja | Bajo | E corre paralelo; synthetic checks toleran transient errors |
| User decision changes mid-execution (e.g., wants real trunk after all) | Baja | Bajo | Spec gates D-FORCEs explicitly; mid-execution course-correct documented in spec amendment |
| **Phase 0LK toma > 3 días** si Talos quirks aparecen (PV provisioning, Cilium NetworkPolicy debugging, hostNetwork conflicts) | Media | Medio | P0LK.1 prereq install incluye fallback a kind si Talos bootstrap stuck > 4h; P0LK escalation gate at day 4 |
| **Kamailio + RTPEngine config knowledge no presente en team** — learning curve no trivial | Media | Alto | Use CyCoreSystems/asterisk-k8s-demo como starting point, no greenfield. P0LK.7 includes "tweak vs adapt" decision gate (4h timebox; si > 4h escalate to user) |
| **docker-compose + K8s datasets divergen tanto que confunde data sheet** | Media | Bajo | F.6 marketing data sheet trata como deployment offerings DIFERENTES (SMB docker-compose tier vs Enterprise K8s tier), no "same product different infra" |
| **Talos cluster + docker-compose paralelo exceede RAM** (60 GB cap) | Baja | Medio | RAM accounting: Talos ~10 GB + observability ~4 GB + apps K8s ~12 GB + docker-compose ~12 GB = 38 GB peak. Margen 22 GB. Si pressure aparece, serializar Phase L → Phase LK |
| **K8s + Asterisk SBC pattern tiene gotcha no anticipado en research** | Media | Alto | Phase 0LK is "discovery + adapt" — extra time scoped (3 días vs idealized 1.5 días). Si gotcha discovered, document + propose fix, no block ship |

---

## Dependencies + sequencing

```
Phase 0L (local docker-compose staging)
   ↓
Phase 0LK (K8s local Talos cluster + Kamailio/RTPEngine SBC)  ──┐
   ↓                                                              │
Phase A (test infra — ALL scripts + Helm charts + manifests)     │
   ↓                                                              │
Phase B-L (baseline docker-local)  ──┐    Phase B-LK (baseline K8s-local) ──┐
                                       │                                       │
Phase C-L (stress + Pumba chaos)  ────┤    Phase C-LK (stress + Chaos Mesh) ─┤
                                       │                                       │
Phase D-L (24h soak docker-local)  ───┘    Phase D-LK (24h soak K8s-local) ──┘
   + Phase E-L paralelo                       + Phase E-LK paralelo
   ↓                                          ↓
   └─────────────┬────────────────────────────┘
                 ↓
        Cloud Replication Checkpoint (BOTH paths green required)
                 ↓
        Phase 0C (cloud provision — docker-compose path)
                 ↓
        Phase B-C → C-C → D-C (48h) + E-C
                 ↓
        Phase F (refresh + closure — 3 datasets integrated)
                 ↓
        R5.5 COMPLETE
```

**Critical path (calendar):** Phase 0L (1.5d) → 0LK (3d) → A (4d) → B-L+B-LK paralelo (3d) → C-L+C-LK paralelo (3d) → D-L+D-LK 24h paralelo (1.5d) + E-L+E-LK paralelo (inline) → 0C (0.5d) → B-C+C-C (3d) → D-C 48h (2d) → F (2d) = ~21 días sin paralelismo agresivo. Con paralelismo Phase L+LK simultaneous = ~18 días.

---

## Aceptación final R5.5

R5.5 se declara COMPLETE cuando:

1. ✅ All phases executed both local + cloud (where applicable)
2. ✅ All ship criteria deliverables shipped (4 docs refrescados + 7 nuevos docs + 7 nuevos scripts/scenarios)
3. ✅ All functional gates passed
4. ✅ Cloud VM torn down + cost reported ≤ $80
5. ✅ R5.5 spec moved active/ → completed/
6. ✅ Memory file written + MEMORY.md indices updated
7. ✅ Marketing data sheet ready for sales handoff
8. ✅ R6 backlog updated con findings que aparecieron pero no se atacaron en R5.5

---

## Próximos pasos inmediatos

1. ✅ Brainstorm R5.5 completado (3 secciones aprobadas, 4 D-FORCEs resueltos)
2. ⏳ User review del presente spec → ajustes si necesario
3. Invocar `superpowers:writing-plans` para crear execution plan companion (`2026-04-27-r5.5-execution-plan.md`)
4. Commit del spec a `docs/plans/active/`
5. Phase 0L dispatch (P0L.1-5 — coordinator foreground, ~1-2 días)

---

## Referencias

- R5 release train final state: `docs/plans/completed/2026-04-22-r5-production-readiness-release-train.md` (DECLARED COMPLETE marker)
- R5.4 ship details: `~/.claude/projects/-media-Data-Source-Verbara-Asterisk-Sdk-Pro/memory/project_r54_production_validation_shipped.md`
- v1 provisional SLOs to refresh: `docs/operations/slos.md`
- v1 provisional capacity to refresh: `docs/operations/capacity-planning.md`
- alerts.yml to validate + tune: `docs/operations/alerts.yml`
- backup/DR runbook with chaos cadence: `docs/operations/backup-disaster-recovery.md`
- NBomber suite (existing): `tests/Verbara.Platform.LoadTests/`
- ADR-0009 SLO baseline policy: `docs/decisions/0009-slo-baseline-alert-severity-model.md`
- Industry research SIPp: https://community.asterisk.org/t/asterisk-stress-testing-using-sipp/83468
- Industry research soak testing: https://www.devzery.com/post/soak-testing-complete-guide-2025
- Industry research Pumba: https://oneuptime.com/blog/post/2026-02-08-how-to-use-docker-for-chaos-engineering-with-pumba/view
- Industry research Asterisk PJSIP benchmarks: https://vitalpbx.com/blog/asterisk-pbx-multicore-4500-calls-test/
- Google SRE PRR pattern: https://sre.google/sre-book/evolving-sre-engagement-model/
- Google SRE SLO + error budget: https://sre.google/workbook/implementing-slos/
- K8s local distros showdown 2025: https://sanj.dev/post/2025-12-11-ultimate-local-kubernetes-showdown-2025
- Talos Linux: https://www.talos.dev/
- Cilium CNI: https://cilium.io/
- MetalLB: https://metallb.universe.tf/
- CloudNativePG operator (CNCF): https://cloudnative-pg.io/
- Chaos Mesh: https://chaos-mesh.org/
- Velero (K8s backup): https://velero.io/
- Asterisk-on-K8s reference: https://github.com/CyCoreSystems/asterisk-k8s-demo
- asterisk-config sidecar (IP injection): https://github.com/CyCoreSystems/asterisk-config
- asterisk-operator (CRD-based scaling): https://github.com/dougbtv/asterisk-operator
- Kamailio K8s deployment guidance: https://www.kamailio.org/events/2018-KamailioWorld/Day1/04-Paolo.Visintin-Kamailio-Docker-Kybernetes.pdf
- Conversation brainstorm origen: 2026-04-27 post-R5.4 Opción A user decision + 2026-04-27 K8s amendment via plan mode
