apiVersion: v1
kind: Service
metadata:
  name: asterisk
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "asterisk.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: asterisk
  ports:
    - name: sip
      port: 5060
      protocol: UDP
    - name: ami
      port: 5038
    - name: ari
      port: 8088
    - name: prom
      port: {{ .Values.asterisk.prometheus.port }}
