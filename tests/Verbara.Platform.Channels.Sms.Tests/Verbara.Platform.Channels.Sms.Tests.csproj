<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\..\src\Verbara.Platform.Channels.Sms\Verbara.Platform.Channels.Sms.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="NSubstitute" />
    <PackageReference Include="FluentAssertions" />
    <PackageReference Include="coverlet.collector" />
    <!-- csat-runner Phase D — Testcontainers Postgres for CsatSmsCorrelator window/collision coverage. -->
    <PackageReference Include="Testcontainers" />
    <PackageReference Include="Npgsql" />
    <PackageReference Include="Verbara.Sdk.Data.Npgsql" />
  </ItemGroup>

  <ItemGroup>
    <!-- csat-runner Phase D — the CsatSmsCorrelator fixture starts a Testcontainers Postgres
         container. The "Connection reset by peer" race that motivated serializing collections
         here was fixed at its source (the fixture's `pg_isready -h 127.0.0.1` probe), so
         `parallelizeTestCollections` is `true`, matching Storage.Postgres.Tests. This project is
         in the REQUIRED fast lane, so its fixture must stay in step with that fix. -->
    <None Include="xunit.runner.json" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
