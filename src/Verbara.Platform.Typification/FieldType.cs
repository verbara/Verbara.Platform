namespace Verbara.Platform.Typification;

/// <summary>Structured capture field input format.</summary>
public enum FieldType
{
    Text = 0,
    Textarea = 1,
    Number = 2,
    Date = 3,
    Boolean = 4,
    Select = 5,
    MultiSelect = 6,
    Phone = 7,
    Lookup = 8,
}
