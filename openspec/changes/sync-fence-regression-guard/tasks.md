## 1. Foundation (Phase A — batch)

- [x] 1.1 Re-confirmed the post-C1 census of banned-class APIs in `tests/**` (11 `Task.Delay`, 0 `Thread.Sleep`/`SpinWait`, 2 `Task.Yield` excluded, 40 `GetAwaiter().GetResult()` excluded) and the 9 prose mentions that must NOT trip — own-grep ground-truthed (agent census said 9; actual is 11)
- [x] 1.2 Added `<PackageVersion Include="Microsoft.CodeAnalysis.CSharp" Version="4.14.0" />` to `Directory.Packages.props` (cached, restores clean on net10.0, 0 NuGet-audit warnings; used as a parsing library, not an analyzer)
- [x] 1.3 Created `tests/Verbara.Platform.Governance.Tests/` (refs `xunit`/`xunit.runner.visualstudio`/`Microsoft.NET.Test.Sdk`/`coverlet.collector`/`FluentAssertions`/`Microsoft.CodeAnalysis.CSharp`; no `ProjectReference`; `GlobalUsings.cs` matches repo convention) + registered in `Verbara.Platform.slnx`. **Checkpoint:** clean `dotnet build -c Release` 0 warnings with a placeholder `[Fact]`

## 2. The guard (Phase B — focused individual subagent; the heart)

- [x] 2.1 `SyncFenceScanner` (internal, pure): `CSharpSyntaxTree.ParseText` → match `InvocationExpressionSyntax` by `(trailing-receiver, method)` ∈ {Task.Delay, Thread.Sleep, Thread.SpinWait, SpinWait.SpinUntil} (handles fully-qualified); best-effort `Stopwatch`-elapsed `while`/`do` spin (gated on a `Stopwatch` token being present); threading-alias / `using static System.Threading.Thread` proxy. Allow-marker = a real `SingleLineCommentTrivia` matching `// fence-allow: CATEGORY — reason` (closed enum + non-empty reason) on the call's line span or the immediately-preceding physical line
- [x] 2.2 `TestTreeSource`: `[CallerFilePath]`-anchored walk to the `.slnx` root → `tests/`; enumerate `*.cs` excluding `obj/`, `bin/`, `*.g.cs`, `*.Designer.cs`, `*.generated.cs`, `<auto-generated>`; throws if root not found (loud, never silently empty)
- [x] 2.3 `SyncFenceRegressionGuardTests`: the gate `[Fact]` (scan tree, assert zero violations, readable per-site message) + liveness (`> 300` files scanned vs ~498 actual) + 19 detector self-tests (true positives, prose/string/XML-doc immunity, marker pass/malformed-reject, fully-qualified, Stopwatch-spin ±, alias proxy, blank-line-leak, string-literal-marker)

## 3. Annotate + verify (Phase C — batch)

- [x] 3.1 Annotated the 11 legitimate sites with `// fence-allow: CATEGORY — reason`: `WorkerResilienceTestHelpers.cs:30` (GUARD-TIMEOUT, kept the :29 prose note), `PostgresHealthCheckTests.cs:40` (SIMULATED-WORK), `HttpRequestNodeHandlerTests.cs:164` (SIMULATED-WORK), 4× `Identity.Redis.Tests` (SETTLE), `ExactlyOnceScenario.cs:59,72` (SETTLE), `PresenceScenario.cs:60` + `QueueIngestionScenario.cs:61` (LOOP-DRIVER)
- [x] 3.2 Guard green: `dotnet test tests/Verbara.Platform.Governance.Tests` 19/19 (all 11 marked, zero violations)
- [x] 3.3 Planted-fence verification: an unmarked `Task.Delay` makes the gate FAIL pointing at it; reverted clean → green again (proves the guard is live against the real tree, not just self-tests)
- [x] 3.4 Full solution `dotnet build -c Release` 0 warnings; the new project runs in CI (not matched by the `Storage.Postgres.Tests`/`Identity.Redis.Tests` exclusion filter)

## 4. Ship

- [x] 4.1 `openspec validate sync-fence-regression-guard --strict` passes
- [x] 4.2 Two-stage review (spec compliance + adversarial code quality) = SHIP-WITH-NITS → hardened I-1 (marker blank-line leak), I-2 (string-literal marker), N-1 (Stopwatch gate); re-verified
- [ ] 4.3 PR → CI green (Build + Unit Tests + Analyze + Dependency Review + Coverage Ratchet + CodeQL) → enqueue → merge
- [ ] 4.4 `openspec archive sync-fence-regression-guard` via its own `docs(openspec):` PR (check 4.3/4.4 first)
