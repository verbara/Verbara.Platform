## 1. Foundation (Phase A — batch)

- [ ] 1.1 Re-confirm the post-C1 census of banned-class APIs in `tests/**` (11 `Task.Delay`, 0 `Thread.Sleep`/`SpinWait`, 2 `Task.Yield` excluded, 40 `GetAwaiter().GetResult()` excluded) and the 9 prose mentions that must NOT trip
- [ ] 1.2 Add `<PackageVersion Include="Microsoft.CodeAnalysis.CSharp" .../>` to `Directory.Packages.props` (cached: 4.14.0 / 5.0.0 / 5.3.0; pick the latest that passes NuGet audit on net10.0)
- [ ] 1.3 Create `tests/Verbara.Platform.Governance.Tests/Verbara.Platform.Governance.Tests.csproj` (refs: `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`, `coverlet.collector`, `FluentAssertions`, `Microsoft.CodeAnalysis.CSharp`; **no `ProjectReference`**) + register it in `Verbara.Platform.slnx`. **Checkpoint:** clean `dotnet build -c Release` (0 warnings) with a trivial passing `[Fact]` BEFORE the guard logic

## 2. The guard (Phase B — focused individual subagent; the heart)

- [ ] 2.1 `SyncFenceScanner` (internal, pure): given source text + path, parse with `CSharpSyntaxTree.ParseText`, find `InvocationExpressionSyntax` whose member access resolves textually to a banned API (`Task.Delay`/`Thread.Sleep`/`Thread.SpinWait`/`SpinWait.SpinUntil`, incl. fully-qualified `System.Threading.*`), plus a best-effort `Stopwatch`-elapsed `while`/`do` spin-loop check; return violations `(file, line, api)` unless an inline `// fence-allow: CATEGORY — reason` marker (closed enum + non-empty reason) is present on the call line or the preceding non-blank line. Also detect threading-alias / `using static System.Threading.Thread` introduction as a proxy violation
- [ ] 2.2 File enumerator: walk `tests/**/*.cs` from a `[CallerFilePath]` repo anchor; exclude `obj/`, `bin/`, `*.g.cs`, `*.Designer.cs`, `*.generated.cs`, `<auto-generated>` headers
- [ ] 2.3 `SyncFenceRegressionGuardTests`: the gate `[Fact]` (scan the tree, assert zero violations with a readable per-site failure message) + liveness `[Fact]`s (scanned > sanity floor of files; planted unmarked fence trips; prose mention does not; well-formed marker passes; malformed/unknown-category marker is rejected)

## 3. Annotate + verify (Phase C — batch)

- [ ] 3.1 Annotate the 11 legitimate sites with `// fence-allow: CATEGORY — reason`: `WorkerResilienceTestHelpers.cs:30` (GUARD-TIMEOUT, convert the existing :29 prose note), `PostgresHealthCheckTests.cs:40` (SIMULATED-WORK), `Flows.Tests/HttpRequestNodeHandlerTests.cs:164` (SIMULATED-WORK), `Identity.Redis.Tests` ×4 (SETTLE), `E2E.Harness/ExactlyOnceScenario.cs:59,72` (SETTLE), `LoadTests/PresenceScenario.cs:60` + `QueueIngestionScenario.cs:61` (LOOP-DRIVER)
- [ ] 3.2 Guard green: `dotnet test tests/Verbara.Platform.Governance.Tests` passes (all sites marked, zero violations)
- [ ] 3.3 Planted-fence verification: temporarily add an unmarked `Task.Delay` to a test file, confirm the guard FAILS pointing at it, then revert
- [ ] 3.4 Full solution `dotnet build -c Release` 0 warnings (`TreatWarningsAsErrors`, `WarningLevel=9999`); confirm the new project is in the CI test set (not matched by the `Storage.Postgres.Tests`/`Identity.Redis.Tests` exclusion filter)

## 4. Ship

- [ ] 4.1 `openspec validate sync-fence-regression-guard --strict` passes
- [ ] 4.2 Two-stage review (spec compliance, then code quality) — SHIP
- [ ] 4.3 PR → CI green (Build + Unit Tests + Analyze + Dependency Review + Coverage Ratchet + CodeQL) → enqueue → merge
- [ ] 4.4 `openspec archive sync-fence-regression-guard` via its own `docs(openspec):` PR (check the boxes first)
