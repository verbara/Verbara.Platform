schema: spec-driven

context: |
  Verbara.Platform — API host and composition root for an omnichannel contact center.
  Stack: .NET 10, C# 14, PostgreSQL 18 (raw Npgsql via Verbara.Sdk.Data.Npgsql — NEVER Dapper),
  Redis (sessions/cache), S3 (media). The Api host (src/Verbara.Platform.Api) publishes Native AOT.
  Consumes Verbara.Sdk (MIT) + Verbara.Sdk.Pro (private) via the local NuGet feed.
  Position in the dependency chain: Sdk -> Sdk.Pro -> Platform <- Platform.Web (API-first).

  Hard constraints (ADR-0022, non-negotiable):
  - Native AOT: no reflection, no Activator.CreateInstance, no dynamic proxies. Use source generators.
  - Every (de)serialized DTO MUST live in a [JsonSerializable] context (ApiJsonContext / RealtimeContractsJsonContext);
    JsonSerializerIsReflectionEnabledByDefault=false.
  - Dapper / Dapper.AOT / Verbara.Sdk.Dapper.Stubs are PERMANENTLY BANNED (BanDapperPackageReferences MSBuild guard).
  - TreatWarningsAsErrors=true, WarningLevel=9999 — zero warnings.
  - Never return anonymous `new {}`; use typed sealed records registered in ApiJsonContext.

  Architecture: dual-scheme auth (JWT RS256 + X-Api-Key), RBAC (permission strings, role templates),
  multi-tenant (X-Tenant-Id header / subdomain wins over JWT tid), endpoint groups mapped in Program.cs
  (a subset feature-gated via FeatureGateService). Middleware order: ErrorHandling -> CORS -> TenantResolution
  -> RateLimiter -> Authentication -> Authorization (invariant: tenant resolution MUST precede rate limiting
  so per-tenant buckets partition correctly — Program.cs is authoritative; post-auth middlewares follow).

  Cross-repo standards (CI gating, context hygiene, branch protection) live in the private verbara-meta repo as ADRs.

rules:
  proposal:
    - "Set a `tier` in frontmatter: GRANDE (cross-repo, >3 days, or a product decision), MEDIANO (one repo, 1-3 days), or PEQUEÑO (bug fix, <1 day). PEQUEÑO may keep the proposal light."
    - "Add product-ownership frontmatter: owner, approver, stakeholder, decision_ref (repo-qualified ADR that motivates or records this change, e.g. Platform/ADR-0033; create or extend an ADR in docs/decisions/ when the change embodies a durable decision). With a single developer, owner = approver."
    - "State the problem (what pain, for whom) for GRANDE and MEDIANO tiers."
    - "Public-repo content rule (verbara-meta/ADR-0005, applies to ALL artifacts of a change): never include secrets/keys/tokens, customer or personal data, absolute machine paths, partner-specific commercial terms, or reproductions of private-repo content — reference private repos only as opaque names (e.g. verbara-meta/ADR-NNNN, Pro/ADR-NNNN); when in doubt the artifact goes in the private repo's docs and the public change carries the opaque reference."
  specs:
    - "Use RFC 2119 keywords: SHALL/MUST for non-negotiable requirements, SHOULD for desirable, MAY for optional."
    - "Write scenarios as GIVEN/WHEN/THEN."
    - "Call out cross-repo impact explicitly along the chain Sdk -> Sdk.Pro -> Platform <- Platform.Web."
    - "Include an Architectural Risk section as an H2 heading ('## Architectural Risk', a sibling of the delta sections — NEVER a '###' header inside requirements: openspec archive rebuilds every '###' as a requirement and aborts): Level (LOW/MEDIUM/HIGH), Affected (areas/repos that can break), Mitigation. Keep SHALL/MUST on the first line of each '### Requirement:'."
  design:
    - "Reference existing ADRs when relevant using the repo/ADR-NNNN format (e.g. Platform/ADR-0022). This applies everywhere an ADR is cited across the repo, not only openspec artifacts: a bare ADR-NNNN refers only to THIS repo's docs/decisions/; a cross-repo citation (e.g. Pro/ADR-0011, Sdk/ADR-0011, verbara-meta/ADR-0006) MUST be repo-qualified — required because ADR numbers collide across repos (operator decision 2026-07-05, verbara-meta/ADR-0011)."
    - "Honor the AOT constraint: no reflection, [JsonSerializable] source-gen for every DTO, NO Dapper — use Verbara.Sdk.Data.Npgsql."
    - "Record any durable architectural decision as an ADR in docs/decisions/, not only in this design."
  tasks:
    - "When the change spans repos, spell out the cross-repo sequence: edit Sdk/Pro -> dotnet pack to local-nuget-feed -> clear NuGet cache -> dotnet restore the consumer."
    - "Verification tasks MUST include `dotnet test` green and CI green, with zero warnings (TreatWarningsAsErrors). EXCEPTION — frontend-only changes hosted here under the hub rule (verbara-meta/ADR-0005): verification is the Web gate set instead (`npm run build`, `npx vitest run`, `npx eslint .`, i18n parity green)."
    - "Use Subagent-Driven Development with FCM batching: Phase A foundation (batch) -> Phase B critical components (focused) -> Phase C integration (batch)."

operations:
  apply:
    guidance:
      - "Do not check off a verification task until it actually ran: `dotnet test` green and CI green, zero warnings. EXCEPTION — a frontend-only change hosted here under the hub rule (verbara-meta/ADR-0005) verifies with the Web gate set instead."
      - "Consuming a new Sdk/Pro build: `dotnet pack` from the producer to the local feed -> clear that package's NuGet cache -> `dotnet restore` here. A pin bump alone restores the stale cached package."
      - "A pin bump is never routine: advancing Verbara.Sdk can drag Microsoft.Extensions / OpenTelemetry floors and produce NU1605, which is fatal under TreatWarningsAsErrors. Read the whole restore diff, not just the line you edited."
      - "Add the `CHANGELOG.md [Unreleased]` entry in this PR; leave its `(#N)` citation for close-out."
      - "Conventional Commits (`feat:`/`fix:`/`docs:`/`refactor:`/`test:`/`chore:`), and never a `Co-Authored-By` trailer."
      - "Anything deferred or discovered mid-apply goes into `tasks.md` or an open change — never only into the PR prose, which nothing reads at close-out."
  archive:
    guidance:
      - "Close out with the CLI: `openspec archive <name> --yes`. Do NOT use `/opsx:archive`, `/opsx:bulk-archive` or `/opsx:sync` — the agent archive path never invokes the CLI, and its no-sync branch moves the change away with the main specs never updated (a later `validate --all --strict` still passes, because the deltas are gone)."
      - "Add `--skip-specs` only if this change already went through a sync. On a change that never synced it silently drops the deltas."
      - "Before archiving: reconcile the living spec, harvest every deferred AND incidentally discovered follow-up into an open change or an ADR addendum, and check off `tasks.md`."
      - "Sweep referrers to the change path before the directory moves — `.github/workflows/`, `scripts/` and test fixtures, not only Markdown."
      - "If archive aborts because this change removes a capability's LAST requirement, that is expected: add `retire_capabilities: true` to the change's `.openspec.yaml`. It DELETES the living spec — use it only for genuine retirement."
      - "Land the close-out as its own `docs(openspec):` PR; `main` is protected. Backfill the feature PR's `(#N)` citation into `CHANGELOG.md [Unreleased]` here — the number did not exist at apply time."
