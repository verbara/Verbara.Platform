global:
  resolve_timeout: 5m

route:
  receiver: console
  group_by: [alertname]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: console
    webhook_configs:
      - url: http://127.0.0.1:8080/__staging-only__
        send_resolved: true
