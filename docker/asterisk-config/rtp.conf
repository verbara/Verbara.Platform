[general]
; Docker compose maps 20000-20200 (100 concurrent calls)
; For higher capacity, expand both here AND in docker-compose port mappings
; Production with host networking can use the full 20000-21999 range
rtpstart = 20000
rtpend = 20200
strictrtp = yes
icesupport = true
